# Legacy v2 regression fixtures

These files document the v2.x architecture and fake-provider contracts. They are intentionally excluded from the v3 default unittest discovery because v3 replaces the monolithic `ui/app.js`, the old prompt wire format, and opaque literal masking. Equivalent v3 behavior is covered by the `tests/test_v3_*.py` suite.
