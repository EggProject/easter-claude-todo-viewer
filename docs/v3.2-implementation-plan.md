# v3.2.0 Implementation Plan

Goal: fix notification/status UI regressions, add Flow auto-layout/filter/sort, retain deleted tasks, and make global-HU translation scheduling event-snapshot based.

1. Add regression tests for nested notification rendering and status multiselect CSS isolation.
2. Add deleted-task public state and event-snapshot translation intent tests.
3. Implement translation intent queue for created/text-changed tasks before present=false can race it.
4. Implement deleted public task representation and history localization.
5. Add ELK auto-layout import and Flow controls (search/status/sort/auto arrange), persistent arranged node positions.
6. Update shared UI styles and modal renderer.
7. Run full Python, bash, JS syntax, package smoke verification.
